import React from "react";
import Carousel from "./carousel/Carousel";
import "./header.scss";

const Header = () => {
  return (
    <header>
      <Carousel />
    </header>
  );
};

export default Header;
