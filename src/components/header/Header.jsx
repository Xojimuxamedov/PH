import React from "react";
import HomeCarousel from "./carousel/Carousel";
import "./header.scss";

const Header = () => {
  return (
    <header>
      <HomeCarousel />
    </header>
  );
};

export default Header;
